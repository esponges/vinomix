import Component, { loader, unstable_shouldReload, ErrorBoundary, CatchBoundary, meta } from "./route-containers/layout/layout.component";

export default Component;
export { loader, unstable_shouldReload, ErrorBoundary, CatchBoundary, meta };
